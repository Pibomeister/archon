RCA review packet ready (the lite lane's only gate): open <this run's ARTIFACTS_DIR>/rca-review.html. Controller planning freeze binding: $rca-freeze.output.binding_id. Frozen oracle digest: $rca-freeze.output.oracle_digest. Controller freeze receipt: $rca-freeze.output.receipt. The HTML packet is reviewer-facing context; the controller receipt and frozen snapshot are the approval authority. Approval covers exactly the rendered and controller-attested plan bound by that freeze; do not edit artifacts at the gate. Approve starts unattended RED -> fix -> negative-control -> review and ends at a draft PR after controller approval verification; this lane has no in-app smoke gate. Reject records feedback only and terminates this guarded run; start a fresh guarded successor using the rejection reason. Abandon ends without rework.
