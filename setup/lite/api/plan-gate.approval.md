Plan review packet ready: open <this run's ARTIFACTS_DIR>/plan-review.html. Controller planning freeze binding: $plan-freeze.output.binding_id. Frozen oracle digest: $plan-freeze.output.oracle_digest. Controller freeze receipt: $plan-freeze.output.receipt. The HTML packet is reviewer-facing context; the controller receipt and frozen snapshot are the approval authority. The run is paused because this stage finished and is waiting on a person; nothing degrades while it waits. The DECIDE box contains provider-appropriate commands. Approve continues only after controller approval verification. Reject records the reason and terminates this guarded run; update inputs and start a fresh guarded run so planning and freeze rerun under a new binding. Abandon ends the run without rework. The routing envelope is rechecked after approval before code is written.
