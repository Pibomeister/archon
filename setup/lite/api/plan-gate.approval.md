Plan review packet ready: open <this run's ARTIFACTS_DIR>/plan-review.html. The run is paused because this stage finished and is waiting on a person; nothing degrades while it waits. The DECIDE box contains provider-appropriate commands. Approve continues to implementation. Reject records the reason and ends this run without rewriting plan.md; a different plan needs a fresh run so the envelope can be rechecked. Abandon ends the run without rework. The routing envelope is rechecked after approval before code is written.
