The human rejected this plan at the gate. Their reason, verbatim:

$REJECTION_REASON

Do not revise the plan, allowlists, reader audits, browser policy, rendered
packet, or any repository file in this run. The controller has frozen the
approval oracle; same-run rework would stale the verified binding and the lite
envelope. This rejection is terminal for the guarded run. Preserve artifacts as
evidence, update rejected inputs outside this run, and start a fresh guarded run
so planning, envelope checks, freeze and approval all rerun under a new binding.
End with: PLAN_REJECTION_REQUIRES_FRESH_GUARDED_RUN
