Compose the W9 draft-PR title and body from approved plan.json, review.json and
verification.json under ARTIFACTS_DIR. Describe the resulting user-visible change,
its verified behavior, and any limitation. Include Summary, Validation, and Known
Residuals sections. Use the original brief and frozen context; invent no results.
The title should state the user benefit or problem being resolved.
Return JSON title/body only. Do not commit, push, create the PR yourself, merge,
deploy, or modify files. A mechanical step owns publication and checks commit-hook
changes before any push. The PR must remain a draft for operator review.
