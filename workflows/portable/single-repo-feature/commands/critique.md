Review the proposed implementation unit supplied below against the frozen brief,
profile scope, policy-context.json and Git-pinned knowledge under ARTIFACTS_DIR.
Read the target files from binding.repositoryRoot in run-context.json. Verify the
mechanism and tests actually address the brief. A CTA-only change must not expand
into billing, extraction, migrations, other applications, or shared packages.

Return approved=true only when the plan is implementable, focused, and its tests
are meaningful; otherwise return concrete blocking findings. Do not edit anything,
accept your own proposal, ask questions, or invoke a gate/control tool.

Proposed plan:
$INPUTS.plan
